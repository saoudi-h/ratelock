# Worklog — 2026-08-22 — BUN-02 + CORE-02

## What was done

### BUN-02 — `bun` driver in @ratelock/redis (DONE)

Implementation in `packages/redis/src/`:
- `types.ts`: `driver` union extended to `'redis' | 'ioredis' | 'bun'`; `RedisClientLike` gains optional `send?(command, args)`; JSDoc updated.
- `client.ts`:
  - `detectDriver`: Bun clients detected FIRST via `'send' in raw && typeof send === 'function'` — must precede the `'ping' in raw` heuristic or Bun clients get misdetected as node-redis.
  - `loadFromUrl('bun')`: dynamic import via non-literal specifier (`const id = 'bun'; import(id)`) so TS needs no bun-types and Node builds don't choke; clear error if not under Bun runtime.
  - `createBunBatch()`: queue-based multi/pipeline over `client.send()`; Bun auto-pipelines concurrent commands on exec (`Promise.all`). All numeric args stringified (`String(keys.length)`, `String(ttlMs)`).
  - Per-method branches for eval/evalsha/loadScript/set/del/pExpire; error messages now mention the bun option.

Verification:
- 13 new unit tests in `packages/redis/tests/client.test.ts` (FakeBunRedis with recorded sends) — run under Node/vitest, no Bun needed: detection, per-command mapping, batch ordering, empty-del alignment, error guidance. 38/38 package tests green.
- REAL Bun verification against Docker Redis (port 6380): both usage modes proven — `driver: 'bun'` via URL AND auto-detected raw `Bun.RedisClient` instance passed as `client`. Fixed window deny path, sliding window, batch all correct.
- README + docs page (`apps/docs/src/content/docs/engines/redis.mdx`) document the new driver.
- Package build + publint clean; tsgo typecheck clean; eslint clean.

### CORE-02 — duplicate-id batch nondeterminism (DONE)

Confirmation (as requested): mechanism re-proven during BUN-01 live runs — concurrent same-key UPSERTs return lock-order-dependent snapshots ([1,2,2] vs [2,2,1]); final DB state always correct; only result VALUES misalign with input positions. Scope confirmed by grep: exactly 6 sites, all in postgres strategies (fw/ifw/tb × pg/postgres.js drivers). Redis needs NO fix — its pipelined EVALSHAs execute server-side in send order (deterministic). Local engine is synchronous — unaffected.

Fix: dedup fallbacks now iterate sequentially (`for...of await`) instead of `Promise.all(ids.map(...))`, keeping single-check semantics for repeated ids. Comment explains why sequential is intentional.

Side-effect verification (as requested):
- New `__tests__/batch-dedup.spec.ts` (4 tests): max-in-flight === 1 during dup fallback for all 3 counter strategies; position-alignment value assertion (second 'a' consumes exactly one more than first); **fast-path guard** proving unique-id batches still use ONE round-trip with zero single upserts.
- Full postgres suite 27/27 (mock contracts + new guards), then real-Postgres integration suite 23/23 via `pnpm test:integration` (Docker up/down automated by script).
- Redis suite 38/38 untouched-green. typecheck/lint/build+publint clean on both packages.

Test-authoring gotcha discovered: my first filter failed because InstrumentedDriver truncated SQL to 40 chars, cutting off 'ON CONFLICT'. Lesson: when asserting on logged SQL, store it untruncated.

## Files modified

- `packages/redis/src/client.ts`, `src/types.ts`, `tests/client.test.ts` (new), `README.md`
- `apps/docs/src/content/docs/engines/redis.mdx`
- `packages/postgres/src/strategies/{fixed-window,token-bucket,individual-fixed-window}/{pg,postgres}.ts` — sequential fallback
- `packages/postgres/__tests__/batch-dedup.spec.ts` (new)
- `.autonomos/TASKS.md` — BUN-02 [x], CORE-02 [x]

## Next steps for the next session

- BENCH-01: extend `packages/bench` harness with a bun-native-driver variant; compare Bun.RedisClient vs node-redis/ioredis under Bun vs Node baseline (docker compose already wired in bench).
- Then decide BUN-03 (Postgres/Bun.SQL) fate from evidence + maintainer's unified-API stance.
