# Worklog — 2026-08-23 — INT-01 release + INT-02 (Express)

## What was done

### INT-01 closed end-to-end
- Style pass on all new artifacts after maintainer review: em-dashes banned everywhere (README, docs page, JSDoc, changeset, AGENT.mds, TASKS.md). Guided by stop-slop skill; table placeholders became `(none)`.
- PR #11 merged → Version Packages PR #12 merged → **@ratelock/hono@0.1.0 published on npm** (17.5 KB unpacked), git tag created.
- Gotcha: `npm view` returned 404 right after the workflow succeeded — registry propagation delay, not a failure. Check logs first, panic later.

### INT-02 Express implemented (PR #13, all checks green)
- Researched exact APIs via context7 before coding (maintainer requirement): confirmed Express 5 auto-forwards promise rejections from middleware Layers while Express 4 does not → our handler always ends with `.catch(next)`, correct on both majors. Confirmed `req.ip` honors `trust proxy`.
- Dual-major testing without duplication: express@4 installed as npm alias (`"express4": "npm:express@^4.21.2"`), loaded via `createRequire` to bypass TS resolution, typed `as typeof express5`. Every scenario runs twice (describe.each).
- 9 scenarios x 2 majors = 18 tests: allow/headers, deny/Retry-After/body, custom status+message, keyGenerator isolation, rfc-only, headers-off, lazy factory deferral (spy), limiter failure forwarded to error handler (503) — the direct proof of v4 compat —, trust proxy identifier isolation via x-forwarded-for.
- Test-authoring notes: supertest absent headers are `undefined` (Hono returns null); eslint `consistent-type-imports` forbids `typeof import()` annotations; a cast before `??` silences no-unnecessary-condition.

## Files

- New: packages/express/** (src, tests incl. dual-major harness, configs, README, LICENSE)
- apps/docs integrations/{meta.json,express.mdx}; .changeset/express-middleware.md (minor → 0.1.0)
- main: TASKS.md roadmap registered (INT-05 Elysia, INT-06 web-standard helper)

## Next steps

- Merge PR #13 → Version Packages PR → @ratelock/express@0.1.0 on npm (authorized end-to-end).
- Then [INT-03] Fastify: research fastify-plugin encapsulation + request.ip, tests via inject().
