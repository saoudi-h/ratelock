# Worklog — 2026-08-22 — INIT-02

## What was done

Triaged legacy `BACKLOG.md` (2026-05-19) and `ANALYSIS_REPORT.md` against the current codebase, with user decisions on each open question.

### Verification results

Items verified DONE in code (removed from tracking):
- Circuit breaker: original error preserved as `cause`; single half-open probe enforced via state machine (second concurrent request gets `CircuitBreakerOpenError`)
- Redis sliding window `windowStart`: Lua returns actual oldest timestamp (`ZRANGE key 0 0 WITHSCORES` → `oldest_ts`)
- Redis `checkBatch`: pipelined EVALSHA with NOSCRIPT reload fallback — all four strategies
- Postgres `checkBatch`: single round-trip via `unnest($1::text[]) WITH ORDINALITY` — all strategies, both drivers
- `invalidate(id)` on cached limiter; cache TTL JSDoc guidance; publint in CI
- Docs UX complete: llms.txt/llms-full.txt/llms.mdx routes, CodeBlockTabs, MarkdownCopyButton, ViewOptionsPopover, PageLastUpdate, not-found.tsx, feedback widget (`rate.tsx`)
- `RUNTIME_COMPATIBILITY.md` was already deleted deliberately (commit `5ef589c`)
- JSDoc: done in core, missing in engine factories → kept as DOC-02

### User decisions

1. **Node engines:** official minimum is **>=22** → ENV-01 registered (engines currently say >=20).
2. **Integration tests:** scaffolds exist (`packages/{redis,postgres}/__integration__/integration.test.ts`) but status unknown → TST-01 "run and assess".
3. **@tala-tools/eslint ESM bug:** fixed upstream — dropped, no issue needed.
4. **Legacy files:** BACKLOG.md + ANALYSIS_REPORT.md deleted (git history preserves them); protocol artifacts are now the single source of truth.
5. **Next big workstream:** Bun 1.4 native clients. Bun 1.4 (released ~2026-08-20) exposes a Redis client API complete enough for this project (incl. Lua/EVALSHA, incomplete before v1.3). Goal: native Bun support for `@ratelock/redis` and `@ratelock/postgres`, deciding between internal drivers vs dedicated packages (aligns with the old decision log: drivers inside existing adapters first). Registered as BUN-01. Middlewares and new adapters confirmed wanted but strictly one thing at a time → backlog INT-01..03, ADP-01..03.

## Files modified

- `.autonomos/TASKS.md` — INIT-02 [x]; registered BUN-01, ENV-01, TST-01; backlog seeded (INT/ADP/CORE/DOC)
- `BACKLOG.md`, `ANALYSIS_REPORT.md` — deleted per user decision
- `.autonomos/worklogs/2026-08-22-INIT-02.md` — this file
- (Earlier same session: AGENT.md root corrections — published v0.2 status, intentional test ports, tsgo/TS7 wait-and-see)

## Next steps for the next session

- Start BUN-01: read Bun 1.4 docs for `Bun.RedisClient` / `bun:sql`-era Postgres client, PoC driver adapters inside existing packages, write decision record.
- Or quick wins first: ENV-01 (mechanical) then TST-01 (needs Docker up).
