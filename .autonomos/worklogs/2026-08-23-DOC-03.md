# Worklog — 2026-08-23 — INT-06 re-scope + DOC-03 (home integrations + recipes)

## What was done

1. **INT-06 parked** per competitor research and maintainer validation:
   - Upstash (@upstash/ratelimit), market leader in our exact position, ships NO framework packages for web-standard frameworks; their documented pattern is the manual 3-line one. Plugins only where real pipelines exist (Strapi).
   - Arcjet's counter-example (per-framework SDKs) is explained by IP-resolution machinery with per-platform proxy trust config (`proxies: [{ip, headers}]`) — a product layer we deliberately do not own.
   - TASKS.md: INT-06 → PARKED with wake criteria; DOC-03 registered.

2. **Framework viability research** (context7, official docs): Next.js App Router (`NextRequest extends Request`), SvelteKit ("transforming Requests into Responses"), Astro ("standard Request interface"), Remix v2 (+ new fetch-router packages) all confirmed native web-standard. H3 v1 event-based = excluded; H3 v2 `event.req` is standard but beta.

3. **Home page IntegrationsSection** (between Engines and Performance): 8 tiles (Hono/Express/Fastify/Elysia as Middleware badges linking to integration pages; Next.js/SvelteKit/Astro/Remix as Guide badges linking to recipes anchors). Brand icons verified against the Iconify API before use (simple-icons: hono/express/fastify; logos: nextjs/svelte/astro/remix; skill-icons: elysia-light). GSAP stagger reveal matching section conventions. Wired into page.tsx.

4. **DOC-03 delivered**: `integrations/framework-recipes.mdx` — shared lazy-init module + header helper, full production patterns for Next.js route handlers, SvelteKit +server.ts, Astro endpoints, Remix loaders, Retry-After computation, per-deployment identifier table (CF-Connecting-IP / Vercel / self-hosted proxy) with spoofing warnings, SolidStart note.

## Gotchas

- Section files import gsap lib from `../../_lib/gsap` at sections/<name>/ depth.
- Fumadocs requires frontmatter title on every MDX page.
- Bare version ranges like `<2` in MDX prose are parsed as JSX: wrap in backticks.
- Process slip: committed homepage work directly on main before catching it; fixed by branching at that commit and resetting main to origin/main. Watch branch state BEFORE staging.

## Validation

Docs build 12/12 · root lint/typecheck clean · cspell clean · PR #20 all checks green, merged.

## Next steps

- Backlog remaining: [CORE-01] circuit breaker state getter, [DOC-01] badges, [DOC-02] engine JSDoc, parked [INT-06]/[BUN-03].
- Candidate follow-up: add a "Guides" cross-link from each middleware page footer to framework-recipes for discoverability.
