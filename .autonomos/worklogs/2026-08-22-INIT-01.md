# Worklog — 2026-08-22 — INIT-01

## What was done

Protocol initialization on the ratelock repo (first autonomos session after install).

1. Oriented: read root `AGENT.md` (empty template), `.autonomos/TASKS.md`, `PROTOCOL.md`, manifest, and all five workflows.
2. Explored the project in depth:
   - Root: README, package.json, pnpm-workspace, BACKLOG.md; git log confirms protocol added as latest commit (`7a76db4`).
   - `packages/core`: v0.2 rewrite — slim contract (`Limiter<T>`) + 4 decorators (`withCache/withRetry/withCircuitBreaker/withFallback`), no strategies/storage inside core anymore. Decorator stacking order matters (fallback→circuitBreaker→retry→cache); only cache preserves `invalidate()`.
   - Engines (`local`, `redis`, `postgres`): one async factory per strategy, identical naming across engines; parity via `@ratelock/test-utils/src/contracts/*`. Postgres has `drivers/`, `migrations.ts`, `cleanup.ts`.
   - Tooling/CI: pnpm+Turbo, tsgo typecheck, tsdown builds, Vitest 4 workspace projects, husky (commitlint + lint-staged), changesets release from main, docs = Next.js 16 + Fumadocs on Vercel. CI gates: Node+Bun test matrix; build/lint/typecheck/publint.

## Key decisions and why

- **Root AGENT.md filled with verified facts** (architecture split core vs engines, definition of done, watch-mode test trap, tsgo, port conventions 6380/5434, unpublished status). Chose facts that will guide different future tasks; left chronology to this worklog.
- **Local AGENT.md files created only where scope-specific rules exist**: `packages/core` (decorator ordering semantics), `packages/{local,redis,postgres}` (engine shape + parity contracts + integration-test constraints), `apps/docs` (ignoreBuildErrors + env validation). No files for `test-utils`/`vitest`/`bench` — their guidance fits the root table; avoid file-count-driven AGENT.md creation per PROTOCOL.md.
- **Created `.autonomos/ISSUES.md` skeleton**: user announced they want to define issues/tasks next session; intake file ready for `/issue`.
- **Registered INIT-02 instead of importing backlog content myself**: legacy `BACKLOG.md` (2026-05-19) and `ANALYSIS_REPORT.md` need triage with the user — some items are already done (e.g. `invalidate(id)` exists in core now), so blind import would create stale tasks.
- Noted discrepancies to surface later: CONTRIBUTING.md says `pnpm changeset` (script doesn't exist); BACKLOG.md claims engines.node >=22 but root package.json says >=20.

## Files modified

- `AGENT.md` (root) — template replaced with real context
- `packages/core/AGENT.md`, `packages/local/AGENT.md`, `packages/redis/AGENT.md`, `packages/postgres/AGENT.md`, `apps/docs/AGENT.md` — new
- `.autonomos/TASKS.md` — INIT-01 → [x], INIT-02 registered
- `.autonomos/ISSUES.md` — new skeleton
- `.autonomos/worklogs/2026-08-22-INIT-01.md` — this file

## Next steps for the next session

- Run `/issue` or direct triage of `BACKLOG.md` + `ANALYSIS_REPORT.md` into ISSUES.md/TASKS.md (INIT-02): verify each item against current code first (several are stale/done).
- Decide with user which framework integration (Hono/Express/Fastify) or adapter work to prioritize.
- Optionally wire cspell into CI or drop it, to be decided during triage.
