# Worklog — 2026-08-23 — INT-03 (Fastify plugin)

## What was done

Third integration, following the established template. PR #15 (5/5 CI) merged; Version Packages PR #16 merged; **@ratelock/fastify@0.1.0 published on npm** (17.8 KB unpacked), git tag created.

- Researched Fastify specifics before coding: `fastify-plugin` wrapper (skips encapsulation so the hook reaches child scopes), `onRequest` hook lifecycle, `request.ip` + `trustProxy`, `inject()` testing.
- Implementation: `rateLimit` plugin adding a global onRequest hook; same option surface as hono/express (instance or lazy factory limiter, keyGenerator override, headers mode, limit for *Limit headers, denyStatusCode, message). Runtime dependency limited to fastify-plugin.
- Dual-major testing: fastify@4 as npm alias (`fastify4`) via createRequire, every scenario runs on 4 and 5 → 20/20 tests.
- Fastify-specific coverage: hook applies to routes in child encapsulation scopes; trustProxy identifier isolation.
- **Framework nuance discovered**: global onRequest hooks run even on unmatched paths (404s) — unlike Express/Hono. Lazy factories therefore initialize on the first request of any kind after boot; documented in README/docs and encoded in the deferral test.

## Gotchas

- fastify-plugin v5 dropped the named export: use default import (`import fp from 'fastify-plugin'`).
- `Parameters<typeof rateLimit>[0]` on a FastifyPluginAsync resolves to FastifyInstance, not options — export/use the named `RateLimitOptions` interface in tests.
- fastify types declare `request.ip: string` (non-optional) → eslint no-unnecessary-condition forbids `?? fallback`; honored types instead.

## Validation

Package 20/20 · root sweep 167/167 across 21 files · build 10/10 tasks · lint/typecheck/publint clean · cspell clean.

## Next steps

- [INT-05] Elysia (pin version range) or [INT-06] web-standard helper — maintainer's pick.
- Rule-of-three check ([INT-04]): resolver/projector/header logic now duplicated across three packages; extraction into private bundled @ratelock/http-common is now justified when convenient.
