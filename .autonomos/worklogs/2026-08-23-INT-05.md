# Worklog — 2026-08-23 — INT-05 (Elysia plugin)

## What was done

Fourth integration, first consumer of the shared `@ratelock/http-common` from day one. PR #18 (5/5 CI) merged; Version Packages PR #19 merged; **@ratelock/elysia@0.1.0 published on npm** (16.5 KB), tag created.

- Researched Elysia API before coding: plugin = `(app: Elysia) => Elysia` mounted via `.use()`, global hook via `.onBeforeHandle({ as: 'global' }, handler)`, early denial by setting `set.status` + returning the body, headers via `set.headers`.
- Peer range pinned `elysia >=1.1 <2` per maintainer instruction (fast-moving ecosystem).
- **Verified empirically that Elysia imports and handles web-standard Requests under Node** (`app.handle(new Request(...))`) — tests run in the standard vitest sweep, no Bun runner needed.
- Default identifier reads Bun `server.requestIP(request)` with shared 'anonymous' fallback; docs explicitly steer proxy users toward keyGenerator and away from spoofable x-forwarded-for.

## Gotchas

- Scaffold hygiene: forgot vitest.config.ts + tsconfig.json on first scaffold → vitest walked up to root projects config and failed confusingly ("No projects were found"). Copy ALL template configs.
- fastify-plugin v5 lesson repeated in a new form: Elysia mutates its phantom generics on hook registration, so `(app) => app.onBeforeHandle(...)` needs an `as unknown as Elysia` cast; consumers see the loose documented shape.
- MDX parses bare `<2` as JSX — version ranges in prose must be wrapped in backticks.
- Elysia's type-level generics also leak into tests: pass instances to helpers structurally (`{ handle: (req) => Promise<Response> }`) instead of the `Elysia` generic type.
- cspell needed "elysia"/"Elysia" additions.

## Validation

Package 9/9 · root sweep 186/186 across 23 files · build 12/12 tasks · lint/typecheck/publint clean · cspell clean after wordlist additions.

## Next steps

- [INT-06] web-standard helper (@ratelock/web) covering Next.js route handlers / SvelteKit / Astro / Remix — now mostly an assembly job since http-common holds the logic and elysia proved the Request/Response path works everywhere.
