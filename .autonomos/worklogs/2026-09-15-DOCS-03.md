# Worklog — DOCS-03 — Fix production site URL

Date: 2026-09-15
Status: done

## Objective
Replace wrong `ratelock.vercel.app` with canonical `https://ratelock-docs.vercel.app` everywhere; determine which published packages need a patch release since npm registry renders README.md.

## Investigation
- `grep ratelock\.vercel\.app`: 15 matches, 0 for `ratelock-docs.vercel.app` before fix.
- Infected published packages (README): `@ratelock/core`, `@ratelock/local`, `@ratelock/redis`, `@ratelock/postgres`, `@ratelock/upstash`.
- Clean (no match): `@ratelock/hono`, `@ratelock/express`, `@ratelock/fastify`, `@ratelock/elysia`, `@ratelock/nestjs` — no patch needed.
- Private packages + docs app: no npm bump needed.
- `apps/docs` dynamic URLs (`metadata.ts`, `sitemap.ts`, `rss.xml`, `og-template`) use `NEXT_PUBLIC_SITE_URL` — no hardcoded wrong domain. Prod value must be verified on Vercel dashboard.
- Other `vercel.app` hit: only external `fumadocs.vercel.app` comment in `source.config.ts` — legitimate.

## Changes
- `README.md`, `packages/{core,local,redis,postgres,upstash}/README.md`, `apps/docs/src/content/docs/getting-started/llm-integration.mdx`: domain-only replacement (paths preserved).
- `.changeset/fix-docs-site-url.md`: single patch changeset covering the 5 infected packages.
- `.autonomos/TASKS.md`: DOCS-03 registered then closed.

## Verification
- `grep ratelock\.vercel\.app`: 0 matches.
- `pnpm exec changeset status`: 5 patch bumps confirmed (core, local, postgres, redis, upstash).
- `git diff --stat`: 8 files, domain-only diff reviewed.
- Full DoD (`build/lint/typecheck/test`) not rerun: README/MDX-only + changeset, no runtime code touched. Publish via normal `release.yml` flow on main.

## Follow-ups / attention
- Vercel prod env: set `NEXT_PUBLIC_SITE_URL=https://ratelock-docs.vercel.app` and redeploy docs.
- Merge to main to let changesets + `release.yml` publish the 5 patches; registry README then self-heals.
- Optional durable guard: record canonical docs URL in `apps/docs/AGENT.md` to prevent regression.
