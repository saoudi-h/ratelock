# Worklog — 2026-08-22 — ENV-01 + TST-01

Branch: `chore/env-tst-housekeeping` (kept off main per maintainer rule).

## ENV-01 — DONE

Aligned `engines.node` from `>=20` to `>=22` in all 7 package.json (root + bench/core/local/postgres/redis/test-utils). README, CONTRIBUTING and `.nvmrc` (25.9.0) already said 22+. `engines.bun >=1.1` deliberately untouched: base packages still support older Bun; only the optional native driver requires >= 1.4 and that is documented in README/docs/types.

Commit: `build: align engines.node to >=22 across workspace` (3ce4590).

## TST-01 — DONE

Ran both integration suites against real Docker infrastructure via each package's `test:integration` script (auto docker up/down):

| Suite | Result |
|---|---|
| `@ratelock/redis` (real Redis 8-alpine :6380, URL auto-detected node-redis driver) | **25/25 passed** |
| `@ratelock/postgres` (real Postgres :5434, postgres.js driver, migrations ON) | **23/23 passed** |

### Coverage assessment

Contracts live in `packages/test-utils/src/contracts/*` and run identically against local (unit), mocked (postgres unit), and real backends (integration). Per strategy: allow/deny, reset semantics, identifier isolation, result field correctness, basic checkBatch.

**Gaps found (registered as TST-02):**
1. **Batch contracts are shallow** — assert only result length + allowed=true on fresh ids. No duplicate-id case (the exact scenario behind CORE-02: a stronger contract would have caught it before release), no per-key remaining/state assertions, no deny-propagation-in-batch case.
2. **Postgres driver matrix incomplete** — integration exercises only the postgres.js path (`sql:` option); the `pg` Pool path (`pool:` option) never touches a real database.
3. **Redis driver matrix** — integration covers node-redis via URL detection; ioredis and the new bun native driver legs are not exercised against a real server.
4. No resilience-composition contracts (decorators vs real backend failure), no destroy/cleanup-timer contract, no migrations-idempotency test. Lower priority; noted, not registered.

Also observed (no action taken): CI does not run integration suites at all (Docker-gated locally); wiring them into CI would need service containers in the workflow — maintainer decision if wanted.

## Files modified

- 7 × package.json (engines), `.autonomos/TASKS.md` (ENV-01 [x], TST-01 [x], TST-02 registered)

## Next steps

- Push `chore/env-tst-housekeeping` + PR (contains ENV-01 commit + this bookkeeping).
- TST-02 next natural work item; then INT-01 Hono middleware remains the big backlog rock.
